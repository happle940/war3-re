# M5 Gate Packet — 内容与视觉身份方向

> 给用户的确认包草稿。当前用于 Codex 预先整理 `M5` 的产品决策输入，不代表 `M5` 已经达到正式决策时点。
> 当前项目处于 `M2-M7 baton mode`：如果用户暂时不做这类产品判断，Codex/GLM 继续推进客观工作，但不能把 `M5` 记成“已决策”。

---

## 目标问题

`M5` 要回答的是：

**这个项目后续到底要长成什么样。**

---

## 人工决策前的准备项

在让用户做 `M5` 决策之前，Codex 至少要准备好这些材料：

- legal asset options documented
- `proxy-first` / `asset-pack-first` / `hybrid` 的成本和风险写清楚
- `one polished Human slice` / `Human vs Orc` / `systems-first sandbox` 的内容范围写清楚
- 当前 gameplay alpha 是否值得继续扩展的结论
- 对下一个产品目标的建议：更好手感 / 更多内容 / 对外 demo

---

## 你需要回答的问题

### 视觉方向

- `proxy-first`
- `asset-pack-first`
- `hybrid`

### 内容范围

- `一个打磨好的人族切片`
- `人族 vs 兽族`
- `系统优先沙盒`

### 下一个产品目标

- `继续打磨手感`
- `扩内容`
- `准备公开 demo`

---

## 默认建议

如果没有新的强约束，默认建议保持：

`hybrid + 一个打磨好的人族切片 + 继续打磨手感`

---

## 当前执行备注

- 本文件当前只是决策 packet 草稿。
- `M5` 依赖前面的 gameplay alpha 足够稳定，否则做内容/视觉路线判断会过早。
