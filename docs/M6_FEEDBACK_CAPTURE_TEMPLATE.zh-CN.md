# M6 Feedback Capture Template：私测反馈表

> 用途：让少量私测反馈可以横向比较。请按字段填写，不需要写成自由长评。

## 基本信息

```text
测试者：
试玩日期：
试玩版本 / 链接：
试玩时长：约 __ 分钟
```

## 设备 / 浏览器 / 版本

```text
设备：桌面 / 笔记本 / 平板 / 其他：
操作系统：
浏览器：
浏览器版本：
屏幕尺寸或大概分辨率：
是否开启浏览器缩放：100% / 其他：
```

## 启动 / 可打开性

```text
能否打开页面：能 / 不能
是否出现白屏：否 / 是
是否启动崩溃：否 / 是
初始 HUD 是否出现：是 / 否 / 不确定
加载到可操作大约耗时：
控制台或页面是否有明显错误：无 / 有：
```

补充说明：

```text

```

## 可读性 / 控制

```text
默认镜头下能否分辨 Town Hall：能 / 不能 / 不确定
默认镜头下能否分辨 worker：能 / 不能 / 不确定
默认镜头下能否分辨金矿和树线：能 / 不能 / 不确定
是否知道开局该先做什么：是 / 否 / 大致知道
worker 能否被选中：能 / 不能
右键地面移动是否有反馈：有 / 没有 / 不确定
右键金矿采集是否开始：是 / 否 / 不确定
Town Hall / Barracks 命令卡是否可读：可读 / 不可读 / 没找到
训练 worker 或 footman 是否成功：成功 / 失败 / 没试到
```

最影响控制或理解的问题：

```text

```

## AI / 对手

```text
AI 是否有采集行为：是 / 否 / 不确定
AI 是否出兵或维持经济：是 / 否 / 不确定
是否感到 AI 不是静止摆设：是 / 否 / 不确定
是否出现 AI 完全停摆：否 / 是 / 不确定
是否出现明显不合理压力：没有 / 太弱 / 太强 / 说不清
```

观察到的 AI 行为：

```text

```

## 困惑 / 阻塞

请只记录最影响有效反馈的 1-3 个问题。

```text
1.
2.
3.
```

是否遇到必须立即停止的问题：

```text
页面打不开 / 白屏 / 启动崩溃：否 / 是
核心对象完全不可见：否 / 是
worker 无法选中或无法移动 / 采集：否 / 是
最小训练闭环无法完成：否 / 是
AI 完全不行动：否 / 是
不可恢复卡死或严重性能问题：否 / 是
HUD / 点击 / 镜头严重污染判断：否 / 是
```

如果选择“是”，请写最短复现步骤：

```text

```

## 不作为最终质量评价的备注

如果你想提美术、内容量、完整阵营、长期平衡或正式 demo 观感，请写在这里，但不要把它作为当前 verdict 的主要依据。

```text

```

## 最终判断

只选一个：

```text
可以继续私测 / 修完再测 / 暂停外发
```

一句理由：

```text

```
